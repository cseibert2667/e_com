import React from 'react'

export default function Profile() {
    //show user posts, info, contact preferences etc. with options to edit, delete
    //messaging feature? notifications? "follow" other users?
    return (
        <div>
            <h1>Profile</h1>
        </div>
    )
}
